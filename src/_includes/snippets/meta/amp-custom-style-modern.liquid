<style amp-custom>
:root {
  --bg: #050607;
  --surface: rgba(10, 14, 20, 0.96);
  --surface-strong: #0e131d;
  --surface-soft: #111821;
  --text: #e8f5ff;
  --muted: #7ac4ff;
  --accent: #66ff9c;
  --accent-soft: rgba(102, 255, 156, 0.18);
  --border: rgba(102, 255, 156, 0.24);
  --shadow: 0 24px 60px rgba(0, 0, 0, 0.25);
  --radius: 0;
  --font-sans: "Mr De Haviland", "Inter", system-ui, sans-serif;
  --font-mono: "VT323", "Cascadia Code", "Courier New", monospace;
}

*, *::before, *::after {
  box-sizing: border-box;
}

html {
  font-family: var(--font-sans);
  font-size: 62.5%;
  -webkit-text-size-adjust: 100%;
  -moz-text-size-adjust: 100%;
  text-rendering: optimizeLegibility;
}

body {
  margin: 0;
  min-height: 100vh;
  background: radial-gradient(circle at top, rgba(59, 127, 255, 0.08), transparent 28%),linear-gradient(180deg, #090b0f 0%, #050607 48%, #070a10 100%);
  color: var(--text);
  line-height: 1.55;
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  background-image: linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
  background-size: 100% 40px, 40px 100%;
  opacity: 0.35;
  z-index: -1;
}

img, amp-img {
  max-width: 100%;
  display: block;
}

a {
  color: var(--accent);
  text-decoration: none;
  transition: color 0.2s ease, transform 0.2s ease;
}

a:hover,
button:hover,
.button:hover,
a:focus-visible,
button:focus-visible,
.button:focus-visible {
  color: #b8fff1;
  outline: none;
}

button,
.button {
  font-family: var(--font-mono);
  font-size: 1.45rem;
  border: 1px solid var(--border);
  background: rgba(102, 255, 156, 0.08);
  color: var(--text);
  padding: 1rem 1.6rem;
  border-radius: 999px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  transition: background 0.25s ease, transform 0.2s ease, border-color 0.2s ease;
  cursor: pointer;
}

.button:hover,
.button:focus-visible {
  background: var(--accent-soft);
  border-color: var(--accent);
  transform: translateY(-1px);
}

.site-shell {
  position: relative;
  padding: 2rem 1.5rem 3rem;
}

.site-wrapper {
  max-width: 1200px;
  margin: 0 auto;
}

.hero {
  padding: 3rem 0;
}

.hero-grid {
  display: grid;
  gap: 2rem;
  grid-template-columns: minmax(0, 1.2fr) minmax(320px, 0.8fr);
  align-items: center;
}

.hero-copy {
  max-width: 46rem;
}

.hero-eyebrow {
  display: inline-block;
  padding: 0.6rem 1.2rem;
  border: 1px solid var(--border);
  border-radius: 999px;
  color: var(--muted);
  font-size: 1.3rem;
  letter-spacing: 0.18em;
  margin-bottom: 1.6rem;
  text-transform: uppercase;
}

.hero-title {
  margin: 0 0 1.4rem;
  font-family: var(--font-mono);
  font-size: 4.4rem;
  line-height: 1.05;
  letter-spacing: -0.04em;
  color: var(--text);
}

.hero-description {
  margin: 0 0 2rem;
  max-width: 44rem;
  font-size: 1.85rem;
  color: #cbe5ff;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
}

.hero-preview {
  position: relative;
  display: grid;
  place-items: center;
}

.hero-preview::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(102, 255, 156, 0.12), transparent 65%);
  pointer-events: none;
  border-radius: 1.6rem;
  filter: blur(12px);
}

.preview-card {
  position: relative;
  overflow: hidden;
  background: linear-gradient(180deg, rgba(8, 14, 22, 0.95), rgba(2, 4, 8, 0.96));
  border: 1px solid rgba(102, 255, 156, 0.16);
  border-radius: 1.8rem;
  box-shadow: var(--shadow);
  min-height: 22rem;
}

.preview-card amp-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.preview-clip {
  clip-path: polygon(0 0, 100% 0, 100% 86%, calc(100% - 40px) 100%, 0 100%);
}

.section-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  margin: 0 0 2rem;
}

.section-heading h2 {
  margin: 0;
  font-family: var(--font-mono);
  font-size: 3rem;
  color: var(--text);
}

.section-heading p {
  margin: 0;
  color: var(--muted);
}

.card-grid {
  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}

.project-card {
  background: var(--surface);
  border: 1px solid rgba(102, 255, 156, 0.12);
  border-radius: 1.6rem;
  padding: 2rem;
  position: relative;
  box-shadow: 0 18px 50px rgba(0, 0, 0, 0.22);
  overflow: hidden;
  transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
}

.project-card:hover,
.project-card:focus-within {
  transform: translateY(-4px);
  border-color: var(--accent);
  box-shadow: 0 28px 70px rgba(0, 0, 0, 0.28);
}

.project-card__title {
  margin: 0 0 0.75rem;
  font-family: var(--font-mono);
  font-size: 2.2rem;
  color: var(--text);
}

.project-card__role {
  margin: 0 0 1.25rem;
  color: var(--muted);
  font-size: 1.4rem;
}

.project-card__image {
  position: relative;
  overflow: hidden;
  border-radius: 1.4rem;
  margin-bottom: 1.5rem;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.06);
}

.project-card__image amp-img {
  width: 100%;
  height: auto;
  display: block;
}

.project-card__body {
  margin: 0;
  color: #d6e9ff;
  font-size: 1.6rem;
  min-height: 8rem;
}

.project-card__tech {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
  margin: 1.5rem 0 0;
}

.tech-pill {
  font-family: var(--font-mono);
  background: rgba(102, 255, 156, 0.06);
  color: var(--accent);
  border: 1px solid rgba(102, 255, 156, 0.18);
  border-radius: 999px;
  padding: 0.55rem 0.9rem;
  font-size: 1.2rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.project-card__footer {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  margin-top: 2rem;
}

.button--ghost {
  background: transparent;
  border-color: rgba(102, 255, 156, 0.24);
}

.button--primary {
  background: var(--accent);
  color: #07110d;
  border-color: var(--accent);
}

footer {
  padding: 3rem 0 1rem;
  text-align: center;
  color: var(--muted);
  font-size: 1.3rem;
}

footer a {
  color: var(--muted);
  text-decoration: none;
}

footer a:hover,
footer a:focus-visible {
  color: var(--accent);
}

#sidebar {
  width: 280px;
  background: rgba(8, 14, 22, 0.98);
  border-right: 1px solid rgba(102, 255, 156, 0.12);
  color: var(--text);
}

.navcolumn {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  padding: 1.5rem;
}

.navcolumn a {
  color: var(--text);
  padding: 0.95rem 1rem;
  border-radius: 0.9rem;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid transparent;
  font-size: 1.45rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.navcolumn a:hover,
.navcolumn a:focus-visible {
  color: var(--accent);
  background: rgba(102, 255, 156, 0.08);
  border-color: rgba(102, 255, 156, 0.15);
}

#idHeader {
  display: grid;
  gap: 1.5rem;
  grid-template-columns: minmax(0, 1fr) minmax(260px, 320px);
  align-items: center;
  margin-bottom: 3rem;
  padding: 2rem 0;
}

#idHeader h1,
#idHeader h6,
#idHeader p {
  margin: 0;
}

#idHeader h1 {
  font-size: 4rem;
  line-height: 1.05;
  color: var(--text);
}

#idHeader h6 {
  color: var(--muted);
  font-size: 1.6rem;
  margin-top: 0.8rem;
}

#idHeader p {
  color: var(--muted);
  font-size: 1.4rem;
  line-height: 1.75;
}

#profilePicture {
  width: 100%;
  max-width: 320px;
  border-radius: 1.8rem;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.08);
}

#profilePicture amp-img {
  display: block;
  width: 100%;
  height: auto;
}

#socList {
  padding: 1.5rem;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.06);
  border-radius: 1.4rem;
}

#socList h1 {
  margin: 0 0 1rem;
  color: var(--accent);
  font-family: var(--font-mono);
  font-size: 2.2rem;
}

#socList p {
  margin: 0;
  line-height: 1.8;
}

#socList a {
  color: var(--muted);
  text-decoration: none;
}

#socList a:hover,
#socList a:focus-visible {
  color: var(--accent);
}

.page-header {
  margin: 0 0 3rem;
}

.page-header .secHeader {
  display: grid;
  gap: 1.2rem;
  grid-template-columns: minmax(0, 1fr) minmax(220px, 1fr);
  align-items: center;
}

.page-header h3 {
  margin: 0;
  font-size: 3rem;
  color: var(--text);
  font-family: var(--font-mono);
}

.page-header h6 {
  margin: 0;
  color: var(--muted);
  font-size: 1.5rem;
}

.bannerWrap {
  width: 100%;
  min-height: 18rem;
  position: relative;
  overflow: hidden;
  border-radius: 1.8rem;
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.03);
}

.bannerWrap amp-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.banner {
  filter: saturate(0.9);
}

@media (max-width: 900px) {
  .hero-grid,
  #idHeader,
  .page-header .secHeader {
    grid-template-columns: 1fr;
  }

  .site-shell {
    padding: 1.5rem 1rem 2rem;
  }

  .hero-title {
    font-size: 3.4rem;
  }
}

@media (max-width: 640px) {
  .topbar {
    flex-direction: column;
    align-items: flex-start;
  }

  .hero-title {
    font-size: 2.8rem;
  }

  .hero-description {
    font-size: 1.6rem;
  }

  .navcolumn {
    padding: 1rem;
  }

  .navcolumn a {
    font-size: 1.3rem;
  }

  .project-card__title {
    font-size: 1.9rem;
  }

  .button {
    width: 100%;
    justify-content: center;
  }

  .hero-actions {
    flex-direction: column;
  }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation: none !important;
    transition: none !important;
  }
}
</style>
