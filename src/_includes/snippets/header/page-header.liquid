    <header id="pageHeader">
			<div class="secHeader">
        <div class="bannerWrap">
          <amp-img src="images/{{page.imagePrefix}}-465_project.png" alt="{{page.imageAlt}}"
            srcset="images/{{page.imagePrefix}}-465_project.png 1024w, images/{{page.imagePrefix}}-354_project.png 768w, images/{{page.imagePrefix}}-240_project.png 320w"
            width="100"
            height="100"
            layout="responsive"
            class="banner"></amp-img>
        </div>
        <h3 class="dropout rightEdge pad">{{page.title}}</h3>
        <h6 class="rightEdge pad">{{page.subtitle}}</h6>
			</div>
    </header>
